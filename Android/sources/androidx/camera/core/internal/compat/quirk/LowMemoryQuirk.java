package androidx.camera.core.internal.compat.quirk;

import java.util.Arrays;
import java.util.HashSet;

public class LowMemoryQuirk implements icc {
    public static final HashSet a = new HashSet(Arrays.asList(new String[]{"SM-A520W", "MOTOG3"}));
}
